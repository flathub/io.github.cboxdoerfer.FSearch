app-id: io.github.cboxdoerfer.FSearch
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: fsearch
finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=fallback-x11
  # Wayland access
  - --socket=wayland
  # Needs to find files everywhere
  - --filesystem=host
cleanup:
  - /share/man
modules:
  - name: fsearch
    buildsystem: meson
    config-opts:
      - -Dbuildtype=release
      - -Dchannel=flathub-stable
    sources:
      - type: git
        url: https://github.com/cboxdoerfer/fsearch.git
        tag: 0.3
        commit: 506e1eded35649f53431a2c79fcf7583c8c2a3f9
